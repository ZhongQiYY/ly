server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    register-with-eureka: false # false不在eureka上注册自己， 默认值为true
    fetch-registry: false # false不拉取服务列表， 默认值为true